node_modules/
dist/
dist-electron/
.DS_Store
*.log
npm-debug.log*
.vite/

# secrets — never commit real keys
.env
.env.local

# machine-local token overlay (deep-merges over tokens.json in brain/tokens.js).
# Holds machine paths (obsidian.vault_path) so no /Users path lands in the tree.
tokens.local.json

# audit artifacts (self-screenshots + playwright scratch)
.playwright-mcp/
slice0-*.png
orb-*.png
rl-*.png
voice-*.png
map-*.png
rework-*.png
baseline-*.png
baseline-*.jpeg
# stage audit shots (s0- sA- sB- sC- sD- sE-) + any jpeg self-screenshots
s[0-9A-E]-*.png
s[0-9A-E]-*.jpeg
*.jpeg

# topo raw sources (re-fetchable; processed <region>.json + admin0_labels.json are committed)
data/topo/land.geojson
data/topo/coastline.geojson
data/topo/boundary_lines.geojson

# captured media (heavy; regenerate with npm run media). MEDIA-INDEX.md is committed.
media/*.png
media/*.gif
media/*.mp4
media/*.webm
media/seq-*/

# PART 6 packaged app (regenerate: npm run pack)
release/

# PART 7 — Kokoro TTS venv + model files (heavy; recreate: voice/kokoro/setup.sh)
voice/kokoro/venv/
voice/kokoro/models/
